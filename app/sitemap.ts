import type { MetadataRoute } from "next";import { products,site,updates } from "@/data/site";export default function sitemap():MetadataRoute.Sitemap{const routes=["","/products","/our-story","/updates","/contact","/privacy","/cookies","/terms",...products.map(p=>`/products/${p.slug}`),...updates.map(u=>`/updates/${u.slug}`)];return routes.map(route=>({url:`${site.url}${route}`,changeFrequency:route===""?"weekly":"monthly",priority:route===""?1:route.startsWith("/products")?.8:.6}))}
