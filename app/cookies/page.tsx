import type { Metadata } from "next";import { LegalPage } from "@/components/legal-page";export const metadata:Metadata={title:"Cookie policy"};export default function Page(){return <LegalPage title="Cookie policy"><h2>Review required</h2><p>This site does not intentionally install analytics or non-essential cookies in its current form. A final policy should be approved and updated if tracking, embedded services or other cookie-setting tools are introduced.</p></LegalPage>}
