"use client";
import { FormEvent, useState } from "react";
export function ContactForm(){const [message,setMessage]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;if(!form.checkValidity()){form.reportValidity();return}setMessage("Thanks — your details are ready, but online delivery is not configured yet. Please email john@onebetterlabs.co.uk directly.")}return <form className="contact-form" onSubmit={submit}><div className="form-grid"><div className="field"><label htmlFor="name">Name</label><input id="name" name="name" required autoComplete="name"/></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="company">Company <span>(optional)</span></label><input id="company" name="company" autoComplete="organization"/></div><div className="field"><label htmlFor="reason">Reason for contact</label><select id="reason" name="reason" required defaultValue=""><option value="" disabled>Select a reason</option><option>Product enquiry</option><option>Partnership</option><option>Early-access interest</option><option>General question</option></select></div><div className="field full"><label htmlFor="message">Message</label><textarea id="message" name="message" required minLength={10}/></div><label className="check field full"><input type="checkbox" required/><span>I’m happy for One Better Labs to use these details to respond to my enquiry.</span></label><div className="field full"><button className="button" type="submit">Prepare enquiry <span>→</span></button><p className="form-note">This preview does not send or store your details. Email delivery will be enabled before launch.</p>{message&&<p className="form-message" role="status">{message}</p>}</div></div></form>}
